[project]
name = "image-publisher"
version = "0.1.0"
description = "Publish image captures to the robot fleet MQTT broker"
requires-python = ">=3.13"
dependencies = [
    "msgpack>=1.2.2",
    "paho-mqtt>=2.1.0",
]

[tool.uv]
package = false
